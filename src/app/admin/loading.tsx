import { AdminPageSkeleton } from "wbl/app/admin/_components/ui";

/** Shown between admin pages; the admin tab bar stays in place. */
export default function Loading() {
  return <AdminPageSkeleton />;
}
